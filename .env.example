# API Configuration
# In development this is the Vite /api proxy target.
# Production uses it directly and requires the same site as the dashboard.
VITE_API_URL=https://api.kulturbytes.de

# App Configuration
VITE_APP_VERSION=1.0.0



